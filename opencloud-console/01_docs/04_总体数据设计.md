# OpenCloud 控制台 — 总体数据设计

> 数据库表结构设计、索引规划与数据字典
> 
> 版本：v1.0 | 最后更新：2026-03-31

---

## 一、数据库选型

| 数据库 | 用途 | 版本 |
|-------|------|------|
| **PostgreSQL** | 主数据库，存储业务数据 | 16.x |
| **Redis** | 缓存、会话、Token 黑名单 | 7.x |
| **Prometheus** | 时序数据，监控指标存储 | 2.x |

---

## 二、数据表总览

| 表名 | 模块 | 说明 | 数据量预估 |
|-----|------|------|-----------|
| sys_user | system | 用户表 | 1,000+ |
| sys_role | system | 角色表 | 100+ |
| sys_user_role | system | 用户角色关联 | 2,000+ |
| sys_menu | system | 菜单/权限表 | 200+ |
| sys_role_menu | system | 角色菜单关联 | 1,000+ |
| sys_dept | system | 部门表 | 100+ |
| sys_tenant | system | 租户表 | 50+ |
| sys_dict_type | system | 字典类型表 | 50+ |
| sys_dict_data | system | 字典数据表 | 500+ |
| sys_operation_log | system | 操作日志表 | 1,000,000+ |
| auth_user_credential | auth | 用户认证凭证表 | 1,000+ |
| auth_login_log | auth | 登录日志表 | 100,000+ |
| monitor_alert_rule | monitor | 告警规则表 | 500+ |
| monitor_alert_record | monitor | 告警记录表 | 10,000,000+ |
| monitor_alert_silence | monitor | 告警静默规则表 | 100+ |
| monitor_notify_channel | monitor | 通知渠道表 | 50+ |
| monitor_notify_log | monitor | 通知日志表 | 1,000,000+ |
| cloud_cluster_config | cloud | 集群配置表 | 20+ |
| cloud_task | cloud | 异步任务表 | 100,000+ |
| cloud_quota | cloud | 租户配额表 | 50+ |
| cloud_sync_log | cloud | 资源同步日志表 | 10,000+ |

---

## 三、核心表设计

### 3.1 认证授权模块 (auth)

#### auth_user_credential（用户认证凭证表）

```sql
CREATE TABLE auth_user_credential (
    id BIGSERIAL PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL UNIQUE,
    username VARCHAR(64) NOT NULL UNIQUE,
    password_hash VARCHAR(128) NOT NULL,
    status VARCHAR(16) NOT NULL DEFAULT 'ACTIVE',
    fail_count SMALLINT NOT NULL DEFAULT 0,
    lock_time TIMESTAMP,
    last_login_time TIMESTAMP,
    last_login_ip VARCHAR(64),
    password_update_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    deleted SMALLINT NOT NULL DEFAULT 0
);
CREATE INDEX idx_auc_username ON auth_user_credential(username) WHERE deleted = 0;
```

#### auth_login_log（登录日志表）

```sql
CREATE TABLE auth_login_log (
    id BIGSERIAL PRIMARY KEY,
    tenant_id VARCHAR(36) NOT NULL,
    username VARCHAR(64) NOT NULL,
    login_ip VARCHAR(64),
    login_location VARCHAR(128),
    browser VARCHAR(128),
    os VARCHAR(128),
    status VARCHAR(16) NOT NULL,
    message VARCHAR(256),
    login_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX idx_all_username ON auth_login_log(username);
CREATE INDEX idx_all_time ON auth_login_log(login_time DESC);
```

### 3.2 系统管理模块 (system)

#### sys_user（用户表）

```sql
CREATE TABLE sys_user (
    id VARCHAR(36) PRIMARY KEY,
    tenant_id VARCHAR(36) NOT NULL DEFAULT 'default',
    dept_id BIGINT,
    username VARCHAR(64) NOT NULL,
    nickname VARCHAR(64) NOT NULL,
    avatar VARCHAR(512),
    email VARCHAR(128),
    phone VARCHAR(16),
    status VARCHAR(16) NOT NULL DEFAULT 'ACTIVE',
    is_builtin BOOLEAN NOT NULL DEFAULT FALSE,
    sort INTEGER NOT NULL DEFAULT 0,
    deleted SMALLINT NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- 索引
CREATE UNIQUE INDEX idx_sys_user_username_tenant ON sys_user(username, tenant_id) WHERE deleted = 0;
CREATE INDEX idx_sys_user_dept ON sys_user(dept_id);
CREATE INDEX idx_sys_user_status ON sys_user(status);
CREATE INDEX idx_sys_user_tenant ON sys_user(tenant_id);
```

#### sys_role（角色表）

```sql
CREATE TABLE sys_role (
    id BIGSERIAL PRIMARY KEY,
    tenant_id VARCHAR(36) NOT NULL DEFAULT 'default',
    role_name VARCHAR(64) NOT NULL,
    role_code VARCHAR(64) NOT NULL,
    description VARCHAR(256),
    is_builtin BOOLEAN NOT NULL DEFAULT FALSE,
    status VARCHAR(16) NOT NULL DEFAULT 'ACTIVE',
    sort INTEGER NOT NULL DEFAULT 0,
    deleted SMALLINT NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE UNIQUE INDEX idx_sys_role_code_tenant ON sys_role(role_code, tenant_id) WHERE deleted = 0;
```

#### sys_menu（菜单/权限表）

```sql
CREATE TABLE sys_menu (
    id BIGSERIAL PRIMARY KEY,
    parent_id BIGINT NOT NULL DEFAULT 0,
    menu_name VARCHAR(64) NOT NULL,
    menu_type SMALLINT NOT NULL, -- 0=目录 1=菜单 2=按钮
    path VARCHAR(256),
    component VARCHAR(256),
    icon VARCHAR(64),
    permission VARCHAR(128),
    visible BOOLEAN NOT NULL DEFAULT TRUE,
    sort INTEGER NOT NULL DEFAULT 0,
    remark VARCHAR(256),
    deleted SMALLINT NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_sys_menu_parent ON sys_menu(parent_id);
CREATE UNIQUE INDEX idx_sm_permission ON sys_menu(permission) WHERE deleted = 0 AND permission IS NOT NULL;
```

#### sys_operation_log（操作日志表）

```sql
CREATE TABLE sys_operation_log (
    id BIGSERIAL PRIMARY KEY,
    user_id VARCHAR(36),
    tenant_id VARCHAR(36),
    username VARCHAR(64),
    module_name VARCHAR(64) NOT NULL,
    operation_type VARCHAR(64) NOT NULL,
    method VARCHAR(8) NOT NULL,
    request_url VARCHAR(512) NOT NULL,
    request_param TEXT,
    response_result TEXT,
    exception_msg TEXT,
    status VARCHAR(16) NOT NULL,
    cost_time INTEGER,
    operate_ip VARCHAR(64),
    operate_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_sol_user ON sys_operation_log(user_id, operate_time DESC);
CREATE INDEX idx_sol_tenant ON sys_operation_log(tenant_id, operate_time DESC);
CREATE INDEX idx_sol_module ON sys_operation_log(module_name);
```

### 3.3 监控告警模块 (monitor)

#### monitor_alert_rule（告警规则表）

```sql
CREATE TABLE monitor_alert_rule (
    id BIGSERIAL PRIMARY KEY,
    tenant_id VARCHAR(36) NOT NULL DEFAULT 'default',
    rule_name VARCHAR(100) NOT NULL,
    promql_expr TEXT NOT NULL,
    severity VARCHAR(20) NOT NULL CHECK (severity IN ('INFO','WARNING','CRITICAL')),
    duration VARCHAR(10) NOT NULL DEFAULT '5m',
    notify_channel_ids TEXT DEFAULT '[]',
    notify_on_resolve BOOLEAN NOT NULL DEFAULT FALSE,
    status VARCHAR(20) NOT NULL DEFAULT 'ENABLED',
    remark VARCHAR(500),
    deleted SMALLINT NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(50),
    updated_by VARCHAR(50)
);

CREATE INDEX idx_alert_rule_status ON monitor_alert_rule(status);
CREATE INDEX idx_alert_rule_severity ON monitor_alert_rule(severity);
CREATE INDEX idx_alert_rule_tenant ON monitor_alert_rule(tenant_id);
```

#### monitor_alert_record（告警记录表）

```sql
CREATE TABLE monitor_alert_record (
    id BIGSERIAL PRIMARY KEY,
    tenant_id VARCHAR(36) NOT NULL DEFAULT 'default',
    rule_id BIGINT,
    alert_name VARCHAR(100) NOT NULL,
    severity VARCHAR(20) NOT NULL,
    instance VARCHAR(255) NOT NULL,
    summary TEXT,
    description TEXT,
    labels TEXT,
    fired_at TIMESTAMP NOT NULL,
    resolved_at TIMESTAMP,
    status VARCHAR(20) NOT NULL DEFAULT 'FIRING',
    acknowledged_by VARCHAR(50),
    acknowledged_at TIMESTAMP,
    acknowledge_comment VARCHAR(500),
    deleted SMALLINT NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- 幂等去重索引
CREATE UNIQUE INDEX idx_alert_record_dedup ON monitor_alert_record(
    alert_name, instance, fired_at
) WHERE deleted = 0;

CREATE INDEX idx_alert_record_status ON monitor_alert_record(status);
CREATE INDEX idx_alert_record_time ON monitor_alert_record(fired_at);
CREATE INDEX idx_alert_record_tenant ON monitor_alert_record(tenant_id);
```

#### monitor_alert_silence（告警静默规则表）

```sql
CREATE TABLE monitor_alert_silence (
    id BIGSERIAL PRIMARY KEY,
    tenant_id VARCHAR(36) NOT NULL DEFAULT 'default',
    silence_name VARCHAR(100) NOT NULL,
    matchers TEXT NOT NULL,
    starts_at TIMESTAMP NOT NULL,
    ends_at TIMESTAMP NOT NULL,
    status VARCHAR(20) NOT NULL DEFAULT 'ACTIVE',
    created_by VARCHAR(50),
    remark VARCHAR(500),
    deleted SMALLINT NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_alert_silence_status ON monitor_alert_silence(status);
CREATE INDEX idx_alert_silence_time ON monitor_alert_silence(starts_at, ends_at);
```

#### monitor_notify_channel（通知渠道表）

```sql
CREATE TABLE monitor_notify_channel (
    id BIGSERIAL PRIMARY KEY,
    tenant_id VARCHAR(36) NOT NULL DEFAULT 'default',
    channel_name VARCHAR(100) NOT NULL,
    channel_type VARCHAR(20) NOT NULL CHECK (channel_type IN ('EMAIL','DINGTALK','WECHAT_WORK','WEBHOOK')),
    config TEXT NOT NULL,
    status VARCHAR(20) NOT NULL DEFAULT 'ENABLED',
    remark VARCHAR(500),
    deleted SMALLINT NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_notify_channel_type ON monitor_notify_channel(channel_type);
CREATE INDEX idx_notify_channel_status ON monitor_notify_channel(status);
```

#### monitor_notify_log（通知日志表）

```sql
CREATE TABLE monitor_notify_log (
    id BIGSERIAL PRIMARY KEY,
    tenant_id VARCHAR(36) NOT NULL DEFAULT 'default',
    alert_record_id BIGINT,
    channel_id BIGINT NOT NULL,
    channel_type VARCHAR(20) NOT NULL,
    notify_target VARCHAR(255) NOT NULL,
    notify_content TEXT,
    status VARCHAR(20) NOT NULL DEFAULT 'PENDING',
    retry_count INT NOT NULL DEFAULT 0,
    error_msg TEXT,
    sent_at TIMESTAMP,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_notify_log_record ON monitor_notify_log(alert_record_id);
CREATE INDEX idx_notify_log_channel ON monitor_notify_log(channel_id);
CREATE INDEX idx_notify_log_status ON monitor_notify_log(status);
CREATE INDEX idx_notify_log_time ON monitor_notify_log(created_at);
```

### 3.4 云平台对接模块 (cloud)

#### cloud_cluster_config（集群配置表）

```sql
CREATE TABLE cloud_cluster_config (
    id BIGSERIAL PRIMARY KEY,
    tenant_id VARCHAR(36) NOT NULL DEFAULT 'default',
    name VARCHAR(64) NOT NULL,
    type VARCHAR(16) NOT NULL CHECK (type IN ('OPENSTACK','CEPH','KUBERNETES')),
    endpoint VARCHAR(256) NOT NULL,
    config_json TEXT NOT NULL,
    status VARCHAR(16) NOT NULL DEFAULT 'PENDING',
    last_check_time TIMESTAMP,
    error_msg VARCHAR(512),
    deleted SMALLINT NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(36) NOT NULL,
    updated_by VARCHAR(36) NOT NULL
);

CREATE UNIQUE INDEX idx_ccc_name ON cloud_cluster_config(tenant_id, name) WHERE deleted = 0;
CREATE INDEX idx_ccc_type ON cloud_cluster_config(type);
CREATE INDEX idx_ccc_status ON cloud_cluster_config(status);
```

#### cloud_task（异步任务表）

```sql
CREATE TABLE cloud_task (
    id BIGSERIAL PRIMARY KEY,
    task_id VARCHAR(36) NOT NULL UNIQUE,
    tenant_id VARCHAR(36) NOT NULL,
    type VARCHAR(32) NOT NULL,
    status VARCHAR(16) NOT NULL DEFAULT 'PENDING',
    resource_type VARCHAR(32) NOT NULL,
    resource_id VARCHAR(64),
    cluster_id VARCHAR(36) NOT NULL,
    progress SMALLINT NOT NULL DEFAULT 0,
    result_json TEXT,
    error_msg VARCHAR(512),
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    started_at TIMESTAMP,
    completed_at TIMESTAMP,
    created_by VARCHAR(36) NOT NULL
);

CREATE INDEX idx_ct_tenant ON cloud_task(tenant_id);
CREATE INDEX idx_ct_status ON cloud_task(status);
CREATE INDEX idx_ct_cluster ON cloud_task(cluster_id);
```

#### cloud_quota（租户配额表）

```sql
CREATE TABLE cloud_quota (
    id BIGSERIAL PRIMARY KEY,
    tenant_id VARCHAR(36) NOT NULL UNIQUE,
    openstack_vcpu_limit INTEGER NOT NULL DEFAULT -1,
    openstack_memory_limit BIGINT NOT NULL DEFAULT -1,
    openstack_instance_limit INTEGER NOT NULL DEFAULT -1,
    openstack_volume_count_limit INTEGER NOT NULL DEFAULT -1,
    openstack_volume_storage_limit BIGINT NOT NULL DEFAULT -1,
    ceph_storage_limit BIGINT NOT NULL DEFAULT -1,
    k8s_cpu_limit INTEGER NOT NULL DEFAULT -1,
    k8s_memory_limit BIGINT NOT NULL DEFAULT -1,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

#### cloud_sync_log（资源同步日志表）

```sql
CREATE TABLE cloud_sync_log (
    id BIGSERIAL PRIMARY KEY,
    cluster_id VARCHAR(36) NOT NULL,
    tenant_id VARCHAR(36) NOT NULL,
    sync_type VARCHAR(16) NOT NULL,
    status VARCHAR(16) NOT NULL,
    resource_counts JSONB,
    error_msg VARCHAR(512),
    started_at TIMESTAMP NOT NULL,
    completed_at TIMESTAMP,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_csl_cluster ON cloud_sync_log(cluster_id);
CREATE INDEX idx_csl_status ON cloud_sync_log(status);
```

---

## 四、公共字段说明

### 4.1 审计字段（所有表通用）

| 字段名 | 类型 | 说明 |
|-------|------|------|
| created_at | TIMESTAMP | 创建时间，默认 CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 更新时间，默认 CURRENT_TIMESTAMP |
| created_by | VARCHAR(50) | 创建人 |
| updated_by | VARCHAR(50) | 更新人 |

### 4.2 逻辑删除字段（所有业务表）

| 字段名 | 类型 | 说明 |
|-------|------|------|
| deleted | SMALLINT | 0=未删除，1=已删除，默认 0 |

### 4.3 多租户字段（所有业务表）

| 字段名 | 类型 | 说明 |
|-------|------|------|
| tenant_id | VARCHAR(36) | 租户标识，默认 'default' |

---

## 五、索引设计原则

### 5.1 索引创建规范

1. **主键索引**：所有表使用 BIGSERIAL 自增主键
2. **唯一索引**：业务唯一字段（如 username + tenant_id）
3. **外键索引**：关联字段自动创建索引
4. **查询索引**：高频查询条件字段
5. **复合索引**：多条件查询场景

### 5.2 索引命名规范

```
idx_{table_name}_{column_name}           -- 单列索引
idx_{table_name}_{col1}_{col2}           -- 复合索引
idx_{table_name}_{column}_tenant         -- 租户隔离索引
```

---

## 六、数据字典

### 6.1 通用状态枚举

| 枚举值 | 含义 | 适用表 |
|-------|------|-------|
| ENABLED | 启用 | sys_user, sys_role, sys_menu, monitor_alert_rule |
| DISABLED | 禁用 | sys_user, sys_role, sys_menu |
| LOCKED | 锁定 | sys_user |

### 6.2 告警相关枚举

| 枚举值 | 含义 | 适用字段 |
|-------|------|---------|
| INFO | 信息 | severity |
| WARNING | 警告 | severity |
| CRITICAL | 严重 | severity |
| FIRING | 触发中 | status |
| RESOLVED | 已恢复 | status |
| ACKNOWLEDGED | 已确认 | status |

### 6.3 通知渠道枚举

| 枚举值 | 含义 |
|-------|------|
| EMAIL | 邮件 |
| DINGTALK | 钉钉 |
| WECHAT_WORK | 企业微信 |
| WEBHOOK | 自定义 Webhook |

---

## 七、附录

### 7.1 相关文档

- [02_总体业务设计.md](./02_总体业务设计.md)
- [03_总体应用设计.md](./03_总体应用设计.md)
- [05_总体技术设计.md](./05_总体技术设计.md)
- [06_数据库脚本.sql](./06_数据库脚本.sql)

---

*文档由 AI 自动生成，后续根据实际开发情况迭代更新。*
