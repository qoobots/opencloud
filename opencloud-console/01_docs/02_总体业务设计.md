# OpenCloud 控制台 — 总体业务设计

> 面向云计算基础设施的统一管理平台业务设计文档
> 
> 版本：v1.0 | 最后更新：2026-03-31

---

## 一、项目定位与目标

### 1.1 项目定位

OpenCloud 控制台是面向**自建机房企业客户**的开源云计算平台管理门户，提供对 OpenStack 虚拟化、Kubernetes 容器编排、Ceph 分布式存储、Prometheus/Grafana 监控等核心基础设施组件的统一可视化管理能力。

### 1.2 业务目标

| 目标层级 | 具体目标 | 衡量指标 |
|---------|---------|---------|
| **战略层** | 降低企业云计算基础设施管理门槛 | 运维效率提升 50%+ |
| **业务层** | 提供类阿里云控制台的用户体验 | 功能完整度 ≥ 80% |
| **技术层** | 实现多云资源的统一纳管与调度 | 支持 3+ 云平台对接 |

### 1.3 目标客户画像

```
┌─────────────────────────────────────────────────────────────┐
│                    目标客户画像                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  企业规模：中大型企业，50+ 服务器规模                        │
│  行业分布：制造业、零售连锁、教育机构、政府机构              │
│  技术特征：有 IT 团队，但云经验有限                          │
│  核心诉求：                                                 │
│    ├─ 降低公有云成本（节省 40-60%）                         │
│    ├─ 数据自主可控（满足等保合规）                          │
│    ├─ 简化运维复杂度（统一管理平台）                        │
│    └─ 支持混合云架构（弹性扩展能力）                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、核心业务领域

### 2.1 业务领域划分

```
┌─────────────────────────────────────────────────────────────────┐
│                    OpenCloud 控制台业务领域                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │  认证授权域  │  │  系统管理域  │  │  云平台对接域 │             │
│  │   (Auth)    │  │  (System)   │  │   (Cloud)   │             │
│  │             │  │             │  │             │             │
│  │ • 用户登录   │  │ • 用户管理   │  │ • OpenStack │             │
│  │ • 权限控制   │  │ • 角色管理   │  │ • Ceph      │             │
│  │ • 会话管理   │  │ • 菜单权限   │  │ • K8s       │             │
│  │ • 操作审计   │  │ • 日志管理   │  │ • 集群配置   │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │  监控告警域  │  │  资源计量域  │  │  安全合规域  │             │
│  │  (Monitor)  │  │ (Metering)  │  │ (Security)  │             │
│  │             │  │             │  │             │             │
│  │ • 指标采集   │  │ • 资源计量   │  │ • 访问控制   │             │
│  │ • 告警管理   │  │ • 账单生成   │  │ • 密钥管理   │             │
│  │ • 通知渠道   │  │ • 成本分析   │  │ • 安全审计   │             │
│  │ • 可视化     │  │ • 计费策略   │  │ • 合规检查   │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 核心业务流程

#### 2.2.1 用户认证流程

```
┌─────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  用户    │────▶│  登录请求   │────▶│  身份验证   │────▶│  生成 Token │
│         │     │  /login     │     │  用户名/密码 │     │  JWT+Refresh│
└─────────┘     └─────────────┘     └─────────────┘     └──────┬──────┘
                                                                │
                                                                ▼
┌─────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  用户    │◀────│  返回 Token │◀────│  Redis 存储 │◀────│  权限加载   │
│         │     │  Access/    │     │  会话信息   │     │  角色/权限  │
│         │     │  Refresh    │     │             │     │             │
└─────────┘     └─────────────┘     └─────────────┘     └─────────────┘
```

**业务规则：**
- 密码采用 BCrypt 加密存储，强度要求：8位以上，含大小写+数字+特殊字符
- 登录失败 5 次锁定账号 30 分钟
- Access Token 有效期 2 小时，Refresh Token 有效期 7 天
- 支持 Token 黑名单（主动登出场景）

#### 2.2.2 资源管理流程（以云主机为例）

```
┌─────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  用户    │────▶│  创建云主机  │────▶│  参数校验   │────▶│  配额检查   │
│         │     │  请求       │     │  名称/规格   │     │  资源限制   │
└─────────┘     └─────────────┘     └─────────────┘     └──────┬──────┘
                                                                │
                                                                ▼
┌─────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  用户    │◀────│  返回结果   │◀────│  更新状态   │◀────│  调用 Nova  │
│         │     │  成功/失败  │     │  数据库记录  │     │  API 创建   │
└─────────┘     └─────────────┘     └─────────────┘     └─────────────┘
```

**业务规则：**
- 创建前校验：名称唯一性、规格合法性、镜像可用性、网络配置
- 配额检查：vCPU、内存、磁盘容量是否超出租户限制
- 异步处理：云主机创建为异步操作，通过 WebSocket 推送状态变更
- 状态流转：创建中 → 运行中/错误

#### 2.2.3 告警处理流程

```
┌─────────────────┐
│  Prometheus     │
│  触发告警       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  AlertManager   │────▶│  Webhook 接收   │────▶│  静默规则检查   │
│  发送告警       │     │  /api/monitor   │     │  匹配标签       │
│                 │     │  /webhook       │     │                 │
└─────────────────┘     └─────────────────┘     └────────┬────────┘
                                                         │
                              ┌──────────────────────────┼──────────────────────────┐
                              │                          │                          │
                              ▼                          ▼                          ▼
                    ┌─────────────────┐        ┌─────────────────┐        ┌─────────────────┐
                    │   命中静默规则   │        │  写入告警记录   │        │  WebSocket      │
                    │   丢弃告警      │        │  monitor_alert  │        │  实时推送       │
                    │                 │        │  _record        │        │                 │
                    └─────────────────┘        └────────┬────────┘        └─────────────────┘
                                                        │
                                                        ▼
                                               ┌─────────────────┐
                                               │  异步通知发送   │
                                               │  邮件/钉钉/企微  │
                                               │  指数退避重试   │
                                               └─────────────────┘
```

**业务规则：**
- 告警去重：基于 (alertName, instance, firedAt) 唯一索引幂等写入
- 静默匹配：多标签 AND 条件匹配，支持时间范围
- 通知策略：P0 立即通知，P1 5分钟内，P2 汇总通知
- 重试机制：最多重试 3 次，指数退避（1s → 2s → 4s）

---

## 三、功能模块详述

### 3.1 认证授权模块（opencloud-auth）

| 功能点 | 业务描述 | 关键规则 |
|-------|---------|---------|
| **用户登录** | 账号密码认证，支持验证码，返回双 Token | 密码错误 5 次锁定 15 分钟，同 IP 限频 10次/分钟 |
| **Token 刷新** | 使用 Refresh Token 换取新 Access Token | Refresh Token 有效期 7 天，单次有效防重放 |
| **用户登出** | Token 加入 Redis 黑名单 | 黑名单 TTL 与 Token 剩余有效期一致 |
| **权限校验** | 基于 RBAC 的方法级权限控制 | 支持 `@PreAuthorize`，权限缓存 10 分钟 |
| **密码修改** | 用户主动修改密码 | 密码强度校验，修改后强制下线所有设备 |
| **强制下线** | 管理员强制踢出用户 | 采用 tokenVersion 机制，无需枚举颁发记录 |

### 3.2 系统管理模块（opencloud-system）

| 功能点 | 业务描述 | 关键规则 |
|-------|---------|---------|
| **用户管理** | 用户的增删改查、状态管理 | admin 账号不可删除/禁用，自操作受限 |
| **角色管理** | 角色定义与权限分配 | 内置角色不可删除/修改编码，角色删除前需解绑用户 |
| **菜单管理** | 菜单树维护、动态路由生成 | 支持目录/菜单/按钮三级，权限标识全局唯一 |
| **部门管理** | 组织架构树维护 | 最大层级 5 层，有子部门或用户归属时不可删除 |
| **字典管理** | 系统枚举字典维护 | 分为字典类型与字典数据两层，系统字典不可删除 |
| **操作日志** | AOP 自动记录操作行为 | 异步写入，保留 180 天，敏感字段自动脱敏 |
| **登录日志** | 登录行为记录与审计 | 由 auth 模块写入，system 模块提供查询视图 |
| **租户管理** | 平台多租户管理 | 仅超管可见，禁用租户将强制下线该租户所有用户 |

### 3.3 云平台对接模块（opencloud-cloud）

| 功能点 | 业务描述 | 关键规则 |
|-------|---------|---------|
| **集群配置** | OpenStack/Ceph/K8s 连接信息管理 | 敏感信息 AES-256-GCM 加密存储，支持连通性测试 |
| **云主机管理** | Nova 虚拟机生命周期管理 | 异步创建，支持从镜像/卷启动，操作前预检配额 |
| **网络管理** | Neutron VPC/子网/安全组/浮动IP管理 | 安全组规则即时生效，浮动 IP 绑定/解绑 |
| **存储管理** | Cinder 卷/快照、Ceph 池/RGW 管理 | 卷扩容需关机，有快照的卷不可直接删除 |
| **K8s 管理** | 工作负载、配置、服务、节点管理 | 生产环境建议只读，Secret 值默认脱敏，支持日志流 |
| **配额管理** | 租户级资源配额设置与检查 | 创建资源前实时查询使用量并校验，支持无限制标识 |
| **状态同步** | 云资源状态定时同步 | 每 5 分钟自动同步，支持手动刷新，增量优先 |
| **异步任务** | 耗时操作的任务跟踪 | 记录任务状态流转，超时自动标记失败 |

### 3.4 监控告警模块（opencloud-monitor）

| 功能点 | 业务描述 | 关键规则 |
|-------|---------|---------|
| **指标查询** | 代理转发 PromQL 查询至 Prometheus | 查询时间范围 ≤ 30 天，支持节点/集群总览预置查询 |
| **Grafana 集成** | 面板嵌入与签名 URL 生成 | 前端 iframe 嵌入，隐藏导航栏（kiosk 模式） |
| **告警规则** | 告警规则 CRUD、PromQL 校验 | 规则变更需同步 AlertManager，支持多渠道绑定 |
| **告警记录** | 告警事件记录与状态流转 | 幂等写入去重，支持人工 ACK，永久保留用于审计 |
| **静默规则** | 告警抑制与时间段控制 | 多标签 AND 匹配，到期自动失效，支持手动停止 |
| **通知渠道** | 邮件/钉钉/企业微信/Webhook | 敏感配置加密存储，发送失败最多指数退避重试 3 次 |
| **WebSocket** | 实时告警推送 | JWT 握手认证，30s 心跳保活，单用户单连接 |

### 3.5 主应用入口模块（opencloud-web）

| 功能点 | 业务描述 | 关键规则 |
|-------|---------|---------|
| **聚合启动** | 整合所有子模块为单一 Spring Boot 应用 | 统一配置数据源、Redis、线程池等全局 Bean |
| **多环境配置** | dev/prod/test 差异化配置 | 生产环境强制关闭 SQL 打印，密钥通过环境变量注入 |
| **数据库迁移** | Flyway 自动执行 DDL 脚本 | 已执行脚本禁止修改，校验 checksum |
| **运维监控** | Actuator 暴露健康检查与指标 | 生产环境仅暴露 health 和 prometheus 端点 |
| **API 文档** | Knife4j 聚合各模块 Swagger 文档 | 按模块分组展示，生产环境可通过配置关闭 |
| **全局异常** | 统一异常捕获与响应封装 | 转换为标准 `R<Void>` 响应，隐藏堆栈详情 |

---

## 四、业务规则汇总

### 4.1 通用业务规则

| 规则类别 | 规则内容 |
|---------|---------|
| **多租户隔离** | 所有数据按 `tenant_id` 字段隔离，查询自动附加租户条件 |
| **逻辑删除** | 所有业务表使用 `deleted` 字段（0=未删除，1=已删除），支持数据恢复 |
| **审计字段** | 所有表包含 `created_at`, `updated_at`, `created_by`, `updated_by` |
| **数据权限** | 支持全部数据、本部门数据、本人数据三级数据权限 |

### 4.2 安全相关规则

| 规则类别 | 规则内容 |
|---------|---------|
| **密码策略** | 8位以上，必须包含大小写字母、数字、特殊字符 |
| **登录锁定** | 连续 5 次失败锁定 30 分钟 |
| **会话管理** | Access Token 2 小时，Refresh Token 7 天 |
| **敏感数据** | 密钥、Token 等使用 AES-256-GCM 加密存储 |
| **操作审计** | 敏感操作（删除、修改配置）必须记录操作日志 |

### 4.3 云平台对接规则

| 规则类别 | 规则内容 |
|---------|---------|
| **连接配置** | 云平台连接信息加密存储，支持连接测试 |
| **配额管理** | 创建资源前校验租户配额，防止超用 |
| **异步操作** | 云资源创建/删除为异步操作，通过轮询或回调更新状态 |
| **状态同步** | 云资源状态定时同步（5 分钟周期），支持手动刷新 |

---

## 五、异常场景处理

### 5.1 认证授权异常

| 异常场景 | 处理策略 |
|---------|---------|
| Token 过期 | 返回 401，前端使用 Refresh Token 刷新 |
| Token 无效 | 返回 401，跳转登录页 |
| 权限不足 | 返回 403，提示无权访问 |
| 账号锁定 | 返回 423，提示剩余锁定时间 |

### 5.2 云平台对接异常

| 异常场景 | 处理策略 |
|---------|---------|
| 云平台连接失败 | 标记集群状态为"离线"，缓存上次成功数据 |
| API 调用超时 | 重试 3 次，超时时间 30s |
| 配额不足 | 前置校验，返回明确错误信息 |
| 云资源不存在 | 同步清理本地数据库记录 |

### 5.3 监控告警异常

| 异常场景 | 处理策略 |
|---------|---------|
| Prometheus 不可达 | 返回缓存数据，标记数据源状态 |
| AlertManager 重复推送 | 幂等写入，基于唯一索引去重 |
| 通知发送失败 | 指数退避重试 3 次，记录失败日志 |
| WebSocket 断开 | 自动重连，恢复订阅状态 |

---

## 六、业务术语表

| 术语 | 英文 | 说明 |
|-----|------|------|
| 租户 | Tenant | 平台资源隔离的基本单位，对应一个企业/组织 |
| 云主机 | Instance | OpenStack Nova 虚拟机实例 |
| 规格 | Flavor | 云主机资源配置模板（vCPU/内存/磁盘） |
| 镜像 | Image | 云主机操作系统模板（Glance 管理） |
| 卷 | Volume | 块存储设备（Cinder 管理） |
| 存储池 | Pool | Ceph 存储资源划分单元 |
| Bucket | Bucket | 对象存储容器（RGW/S3 概念） |
| 命名空间 | Namespace | Kubernetes 资源隔离单元 |
| 工作负载 | Workload | Kubernetes Deployment/StatefulSet/DaemonSet |
| PromQL | PromQL | Prometheus 查询语言 |
| 告警规则 | Alert Rule | Prometheus 告警触发条件定义 |
| 静默规则 | Silence Rule | 告警抑制规则，临时屏蔽特定告警 |

---

## 七、附录

### 7.1 参考文档

- [01_头脑风暴.md](../02_云计算平台/01_头脑风暴.md)
- [02_总体工作清单.md](../02_云计算平台/02_总体工作清单.md)

### 7.2 相关模块文档

- [opencloud-auth/02_业务设计.md](../opencloud-auth/01_docs/02_业务设计.md)
- [opencloud-system/02_业务设计.md](../opencloud-system/01_docs/02_业务设计.md)
- [opencloud-monitor/02_业务设计.md](../opencloud-monitor/01_docs/02_业务设计.md)
- [opencloud-cloud/02_业务设计.md](../opencloud-cloud/01_docs/02_业务设计.md)

---

*文档由 AI 自动生成，后续根据实际开发情况迭代更新。*
