# opencloud-ui — 开发进度

> 模块职责：Vue 3 + Vite 前端工程，提供 OpenCloud 控制台的完整用户界面，包括认证、资源管理、监控告警、系统管理等所有业务页面。
>
> 最后更新：2026-03-31

---

## 功能清单

### 基础骨架

| 文件/功能 | 描述 | 状态 |
|-----------|------|------|
| Vite 5 项目初始化 | TypeScript + Vue 3 + 路径别名 `@` + 代理配置 | ✅ 完成 |
| Element Plus 集成 | 自动按需导入（unplugin-vue-components） | ✅ 完成 |
| Axios 封装 | `src/utils/request.ts`：统一 baseURL、请求头、401 自动刷新 Token、响应错误提示 | ✅ 完成 |
| Pinia 用户 Store | `src/stores/user.ts`：登录/登出/用户信息/权限码/菜单 | ✅ 完成 |
| Vue Router | `src/router/index.ts`：完整路由树 + 登录守卫 + NProgress 进度条 | ✅ 完成 |
| 全局样式 | `src/styles/index.css`：CSS 变量、主色调（蓝色科技感）、Element Plus 覆盖 | ✅ 完成 |
| 主布局 | `src/layouts/MainLayout.vue`：侧边栏（可折叠）+ 顶栏 + 面包屑 + 用户下拉菜单 | ✅ 完成 |

### 认证模块

| 页面/功能 | 描述 | 状态 |
|-----------|------|------|
| 登录页 | `views/auth/LoginView.vue`：暗色玻璃态设计，用户名密码表单 + 表单校验 | ✅ 完成 |
| 登出 | 顶栏下拉菜单调用登出接口，清除本地 Token + 跳转登录页 | ✅ 完成 |
| 路由守卫 | 未登录跳转 `/login`，已登录访问 `/login` 跳转首页 | ✅ 完成 |
| 权限码控制 | `v-permission` 指令（按钮级权限显隐） | 🔲 待开发 |
| 动态路由 | 登录后根据后端菜单动态注册路由 | 🔲 待开发 |

### 仪表盘总览

| 页面/功能 | 描述 | 状态 |
|-----------|------|------|
| Dashboard 页面 | `views/dashboard/DashboardView.vue` | ✅ 完成 |
| 统计卡片 | 云主机数/节点数/存储容量/告警数 4 张统计卡 | ✅ 完成 |
| 资源趋势折线图 | ECharts：CPU/内存/网络 7 天趋势 | ✅ 完成 |
| 存储分布饼图 | ECharts：Ceph 各存储池占比 | ✅ 完成 |
| 告警列表 | 最近 10 条告警（级别/内容/时间） | ✅ 完成 |
| 接入真实数据 | 调用后端 `/api/dashboard/overview` 接口替换 mock 数据 | 🔲 待开发 |

### 系统管理页面

| 页面 | 描述 | 状态 |
|------|------|------|
| 用户管理 | 分页表格 + 搜索筛选 + 新增/编辑 Dialog + 角色分配 + 重置密码 | 🔲 待开发 |
| 角色管理 | 列表 + 新增/编辑 + 权限树（树形 CheckBox） | 🔲 待开发 |
| 菜单管理 | 树形表格展示 + 新增/编辑（目录/菜单/按钮）+ 图标选择器 | 🔲 待开发 |
| 操作日志 | 分页查询 + 详情弹窗 + 条件筛选（用户/模块/时间）+ 导出 | 🔲 待开发 |
| 登录日志 | 分页查询 + IP/浏览器信息 | 🔲 待开发 |

### OpenStack 管理页面

| 页面 | 描述 | 状态 |
|------|------|------|
| 云主机列表 | 卡片/表格切换 + 状态筛选 + 启动/停止/重启/删除操作 | 🔲 待开发 |
| 创建云主机 | 步骤表单（规格/镜像/网络/安全组/配置） | 🔲 待开发 |
| 网络管理 | VPC / 子网 / 安全组列表与管理 | 🔲 待开发 |
| 镜像管理 | 镜像列表 + 上传 + 详情 | 🔲 待开发 |
| 存储卷管理 | 卷列表 + 挂载/卸载 + 快照管理 | 🔲 待开发 |

### Ceph 存储管理页面

| 页面 | 描述 | 状态 |
|------|------|------|
| 集群总览 | 容量仪表盘 + OSD 状态矩阵 + 健康状态 | 🔲 待开发 |
| 存储池列表 | 池容量/PG 数/副本数/压缩信息 | 🔲 待开发 |
| 对象存储 | Bucket 列表 + 文件浏览器 + 上传/下载 | 🔲 待开发 |

### Kubernetes 管理页面

| 页面 | 描述 | 状态 |
|------|------|------|
| 工作负载总览 | Deployment / StatefulSet / DaemonSet 列表 | 🔲 待开发 |
| Pod 管理 | Pod 列表 + 状态 + 实时日志（SSE） | 🔲 待开发 |
| Service 管理 | Service 列表 + 端口信息 | 🔲 待开发 |
| 配置管理 | ConfigMap / Secret CRUD | 🔲 待开发 |
| 节点管理 | 节点列表 + 资源使用率 | 🔲 待开发 |

### 监控告警页面

| 页面 | 描述 | 状态 |
|------|------|------|
| 监控大盘 | Grafana iframe 嵌入 + 面板切换 | 🔲 待开发 |
| 指标探索 | PromQL 输入 + 图表可视化（ECharts） | 🔲 待开发 |
| 告警列表 | 实时告警（WebSocket）+ 分页历史记录 + 手动确认 | 🔲 待开发 |
| 告警规则 | 规则 CRUD + 阈值配置 + 关联通知渠道 | 🔲 待开发 |
| 通知渠道 | 渠道管理 + 测试发送 | 🔲 待开发 |

---

## 文件结构

```
opencloud-ui/src/
├── api/                      # 按模块封装的 Axios 请求函数
│   ├── auth.ts               （待开发）
│   ├── system.ts             （待开发）
│   ├── cloud.ts              （待开发）
│   └── monitor.ts            （待开发）
├── assets/                   # 静态资源（图标、图片）
├── components/               # 公共组件
│   ├── StatusBadge.vue       （待开发）
│   ├── MetricCard.vue        （待开发）
│   ├── PermissionButton.vue  （待开发）
│   └── IconSelector.vue      （待开发）
├── directives/               # 自定义指令
│   └── permission.ts         v-permission（待开发）
├── hooks/                    # 可复用 Composable
│   ├── useTable.ts           （待开发，封装分页表格逻辑）
│   └── useWebSocket.ts       （待开发，告警 WS 连接）
├── layouts/
│   └── MainLayout.vue        ✅
├── router/
│   └── index.ts              ✅
├── stores/
│   ├── user.ts               ✅
│   └── alert.ts              （待开发，告警实时状态）
├── styles/
│   └── index.css             ✅
├── utils/
│   └── request.ts            ✅
└── views/
    ├── auth/
    │   └── LoginView.vue     ✅
    ├── dashboard/
    │   └── DashboardView.vue ✅
    ├── system/               （待开发）
    ├── openstack/            （待开发）
    ├── ceph/                 （待开发）
    ├── kubernetes/           （待开发）
    └── monitor/              （待开发）
```

---

## 开发规范

- **组件命名**：PascalCase，文件名与组件名一致
- **API 函数**：每个模块一个文件，函数名 `get/create/update/delete` 前缀
- **TypeScript**：所有接口请求参数和响应类型需定义 `interface`，放在对应 `api/*.ts` 文件末尾
- **样式**：优先使用 Element Plus 变量，自定义样式写在 `<style scoped>` 中，全局样式只在 `index.css` 修改

---

## 本地开发

```bash
cd opencloud-ui
npm install
npm run dev         # 启动开发服务器（http://localhost:3000）
npm run build       # 构建生产包（dist/）
npm run preview     # 预览生产构建
```

Vite 开发服务器已配置代理：
- `/api/**` → `http://localhost:8080`（Spring Boot 后端）
