# opencloud-monitor 技术设计文档

---

## 1. 技术栈

| 层次 | 技术选型 | 版本 / 说明 |
|------|---------|-----------|
| Web 框架 | Spring Boot | 3.2.x |
| 异步 HTTP | Spring WebFlux（WebClient） | 3.2.x，用于 Prometheus / Grafana / 钉钉 / 企微 API 调用 |
| WebSocket | Spring WebSocket（原生 STOMP-free） | TextWebSocketHandler，无 STOMP |
| ORM | MyBatis-Plus | 3.5.x |
| 数据库 | PostgreSQL | 15+ |
| 消息缓存 | JVM 内存（ConcurrentHashMap） | WebSocket Session 注册表，v1 单节点 |
| 邮件 | Spring Boot Mail | JavaMailSender |
| 任务调度 | Spring @Scheduled | 心跳保活 + 静默规则过期检查 |
| 加密 | JDK AES-256-GCM | 通知渠道敏感字段加密 |
| 模板引擎 | Freemarker | Webhook 消息体模板渲染 |

---

## 2. Maven 依赖

```xml
<!-- opencloud-monitor/pom.xml（已有） -->
<dependencies>
    <dependency>
        <groupId>com.qoobot.opencloud</groupId>
        <artifactId>opencloud-common</artifactId>
    </dependency>
    <!-- WebSocket -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-websocket</artifactId>
    </dependency>
    <!-- WebClient（Prometheus/Grafana/钉钉/企微 HTTP 调用） -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-webflux</artifactId>
    </dependency>
    <!-- MyBatis-Plus -->
    <dependency>
        <groupId>com.baomidou</groupId>
        <artifactId>mybatis-plus-boot-starter</artifactId>
    </dependency>
    <!-- PostgreSQL -->
    <dependency>
        <groupId>org.postgresql</groupId>
        <artifactId>postgresql</artifactId>
        <scope>runtime</scope>
    </dependency>
    <!-- 邮件 -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-mail</artifactId>
    </dependency>
    <!-- Freemarker（Webhook 模板渲染） -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-freemarker</artifactId>
    </dependency>
</dependencies>
```

---

## 3. 核心技术实现

### 3.1 PrometheusClient（WebClient 封装）

```java
@Component
public class PrometheusClient {

    private final WebClient webClient;

    public PrometheusClient(
            @Value("${opencloud.prometheus.url}") String prometheusUrl,
            @Value("${opencloud.prometheus.timeout:10000}") long timeoutMs) {
        this.webClient = WebClient.builder()
                .baseUrl(prometheusUrl)
                .codecs(c -> c.defaultCodecs().maxInMemorySize(10 * 1024 * 1024))
                .build();
    }

    /**
     * 即时查询：GET /api/v1/query?query={expr}&time={time}
     */
    public Map<String, Object> query(String expr, Long time) {
        return webClient.get()
                .uri(uriBuilder -> uriBuilder.path("/api/v1/query")
                        .queryParam("query", expr)
                        .queryParamIfPresent("time", Optional.ofNullable(time))
                        .build())
                .retrieve()
                .onStatus(HttpStatusCode::isError, resp ->
                        resp.bodyToMono(String.class)
                                .map(body -> new MonitorException(MONITOR_1002, "PromQL error: " + body)))
                .bodyToMono(new ParameterizedTypeReference<Map<String, Object>>() {})
                .timeout(Duration.ofMillis(timeoutMs))
                .onErrorMap(TimeoutException.class, e -> new MonitorException(MONITOR_1003, "查询超时"))
                .onErrorMap(WebClientRequestException.class, e -> new MonitorException(MONITOR_1001, "Prometheus 不可达"))
                .block();
    }

    /**
     * 范围查询：GET /api/v1/query_range
     */
    public Map<String, Object> queryRange(String expr, long start, long end, String step) {
        // 时间范围校验
        if ((end - start) > 30L * 24 * 3600) {
            throw new MonitorException(MONITOR_1004, "时间范围不能超过 30 天");
        }
        return webClient.get()
                .uri(uriBuilder -> uriBuilder.path("/api/v1/query_range")
                        .queryParam("query", expr)
                        .queryParam("start", start)
                        .queryParam("end", end)
                        .queryParam("step", step)
                        .build())
                .retrieve()
                .bodyToMono(new ParameterizedTypeReference<Map<String, Object>>() {})
                .timeout(Duration.ofMillis(timeoutMs))
                .onErrorMap(TimeoutException.class, e -> new MonitorException(MONITOR_1003, "查询超时"))
                .block();
    }

    /**
     * 获取指标名称列表：GET /api/v1/label/__name__/values
     */
    public List<String> labelValues(String prefix) {
        Map<String, Object> resp = webClient.get()
                .uri("/api/v1/label/__name__/values")
                .retrieve()
                .bodyToMono(new ParameterizedTypeReference<Map<String, Object>>() {})
                .timeout(Duration.ofMillis(timeoutMs))
                .block();
        List<String> all = (List<String>) ((Map<?,?>)resp).getOrDefault("data", List.of());
        if (StringUtils.hasText(prefix)) {
            return all.stream().filter(s -> s.startsWith(prefix)).collect(Collectors.toList());
        }
        return all;
    }

    /**
     * 校验 PromQL 语法（试执行，不关注返回值）
     */
    public void validateExpr(String expr) {
        try {
            query(expr, null);
        } catch (MonitorException e) {
            if (e.getCode() == MONITOR_1002) throw e;
            // Prometheus 不可达时不阻塞规则创建，仅记录
        }
    }
}
```

---

### 3.2 WebSocket 实现

#### 3.2.1 WebSocketConfig

```java
@Configuration
@EnableWebSocket
public class WebSocketConfig implements WebSocketConfigurer {

    @Autowired
    private AlertWebSocketHandler alertWebSocketHandler;

    @Override
    public void registerWebSocketHandlers(WebSocketHandlerRegistry registry) {
        registry.addHandler(alertWebSocketHandler, "/ws/alerts")
                .setAllowedOriginPatterns("*");  // 生产环境配置具体域名
    }
}
```

#### 3.2.2 AlertWebSocketHandler

```java
@Component
public class AlertWebSocketHandler extends TextWebSocketHandler {

    @Autowired
    private JwtUtil jwtUtil;
    @Autowired
    private WebSocketSessionRegistry sessionRegistry;

    @Override
    public void afterConnectionEstablished(WebSocketSession session) throws Exception {
        // 从 URI 中提取 token 参数
        String query = session.getUri().getQuery();  // token=xxx
        String token = extractToken(query);
        if (token == null || !jwtUtil.validateToken(token)) {
            session.close(CloseStatus.NOT_ACCEPTABLE.withReason("Token 无效"));
            return;
        }
        String userId = jwtUtil.getUserId(token);
        sessionRegistry.register(userId, session);
        // 发送握手确认
        session.sendMessage(new TextMessage("""
            {"type":"CONNECTED","message":"WebSocket 连接成功"}"""));
    }

    @Override
    protected void handleTextMessage(WebSocketSession session, TextMessage message) {
        // 处理客户端发来的 PONG 心跳，更新最后活跃时间
        // 其他消息忽略
    }

    @Override
    public void afterConnectionClosed(WebSocketSession session, CloseStatus status) {
        sessionRegistry.unregisterBySession(session);
    }

    @Override
    public void handleTransportError(WebSocketSession session, Throwable exception) {
        log.warn("WebSocket transport error: {}", exception.getMessage());
        sessionRegistry.unregisterBySession(session);
    }
}
```

#### 3.2.3 WebSocketSessionRegistry

```java
@Component
public class WebSocketSessionRegistry {

    // userId -> WebSocketSession
    private final ConcurrentHashMap<String, WebSocketSession> sessions = new ConcurrentHashMap<>();
    // session.getId() -> userId（反向索引，用于断连时找 userId）
    private final ConcurrentHashMap<String, String> sessionIdToUserId = new ConcurrentHashMap<>();

    public void register(String userId, WebSocketSession newSession) {
        WebSocketSession old = sessions.put(userId, newSession);
        if (old != null && old.isOpen()) {
            try { old.close(); } catch (Exception ignored) {}
        }
        sessionIdToUserId.put(newSession.getId(), userId);
    }

    public void unregisterBySession(WebSocketSession session) {
        String userId = sessionIdToUserId.remove(session.getId());
        if (userId != null) {
            sessions.remove(userId, session);
        }
    }

    public void broadcast(String message) {
        sessions.values().forEach(session -> {
            if (session.isOpen()) {
                try {
                    session.sendMessage(new TextMessage(message));
                } catch (Exception e) {
                    unregisterBySession(session);
                }
            }
        });
    }

    public int getActiveCount() { return sessions.size(); }
}
```

#### 3.2.4 心跳任务

```java
@Component
public class WebSocketHeartbeatTask {

    @Autowired
    private WebSocketSessionRegistry registry;

    @Scheduled(fixedDelay = 30_000)
    public void sendPing() {
        registry.broadcast("{\"type\":\"PING\"}");
    }
}
```

---

### 3.3 AlertWebhookServiceImpl（核心流程）

```java
@Service
@Slf4j
public class AlertWebhookServiceImpl implements AlertWebhookService {

    @Autowired private AlertRecordMapper recordMapper;
    @Autowired private AlertRuleMapper ruleMapper;
    @Autowired private AlertSilenceMapper silenceMapper;
    @Autowired private AlertPushService pushService;
    @Autowired private NotifyDispatchService notifyDispatch;

    @Override
    @Transactional
    public void handleWebhook(AlertManagerPayloadDTO payload, String webhookSecret) {
        // 签名校验
        validateSignature(payload, webhookSecret);

        for (AlertManagerAlertDTO alert : payload.getAlerts()) {
            if ("firing".equals(alert.getStatus())) {
                handleFiring(alert);
            } else if ("resolved".equals(alert.getStatus())) {
                handleResolved(alert);
            }
        }
    }

    private void handleFiring(AlertManagerAlertDTO alert) {
        String alertName = alert.getLabels().get("alertname");
        String instance  = alert.getLabels().getOrDefault("instance", "");

        // 幂等写入
        MonitorAlertRecord existing = recordMapper.findByDedup(alertName, instance, alert.getStartsAt());
        if (existing != null) {
            log.debug("告警已存在，跳过写入：{} @ {}", alertName, instance);
            return;
        }

        // 匹配规则
        MonitorAlertRule rule = ruleMapper.findByAlertName(alertName, TenantContext.get());
        String severity = rule != null ? rule.getSeverity()
                : alert.getLabels().getOrDefault("severity", "WARNING").toUpperCase();

        // 写入告警记录
        MonitorAlertRecord record = buildRecord(alert, rule, severity);
        recordMapper.insert(record);

        // 检查静默
        if (isSilenced(alert.getLabels())) {
            log.info("告警命中静默规则，跳过通知：{}", alertName);
            return;
        }

        // WebSocket 广播（同步）
        pushService.broadcast(buildFiringEvent(record));

        // 异步发送通知
        if (rule != null) {
            notifyDispatch.sendAsync(record, rule.getNotifyChannelIds());
        }
    }

    private void handleResolved(AlertManagerAlertDTO alert) {
        String alertName = alert.getLabels().get("alertname");
        String instance  = alert.getLabels().getOrDefault("instance", "");
        // 查找匹配的 FIRING / ACKNOWLEDGED 记录
        MonitorAlertRecord record = recordMapper.findFiringByAlertAndInstance(alertName, instance);
        if (record == null) {
            log.warn("未找到对应 FIRING 记录，忽略 resolved：{}", alertName);
            return;
        }
        record.setStatus("RESOLVED");
        record.setResolvedAt(alert.getEndsAt());
        recordMapper.updateById(record);

        // 恢复通知（若规则配置了 notifyOnResolve）
        MonitorAlertRule rule = rule(record.getRuleId());
        if (rule != null && rule.isNotifyOnResolve()) {
            notifyDispatch.sendResolvedAsync(record, rule.getNotifyChannelIds());
        }
        // WebSocket 广播恢复事件
        pushService.broadcast(buildResolvedEvent(record));
    }

    private boolean isSilenced(Map<String, String> labels) {
        List<MonitorAlertSilence> silences = silenceMapper.findActiveSilences(TenantContext.get());
        return silences.stream().anyMatch(s -> matchLabels(s.getMatchLabels(), labels));
    }
}
```

---

### 3.4 NotifySender 策略实现

#### 3.4.1 DingTalkNotifySender（加签）

```java
@Component
public class DingTalkNotifySender implements NotifySender {

    @Override
    public ChannelType supportType() { return ChannelType.DINGTALK; }

    @Override
    public void send(NotifyContext ctx) {
        DingTalkConfig config = ctx.channelConfig(DingTalkConfig.class);
        String webhookUrl = buildSignedUrl(config.getWebhookUrl(), config.getSecret());
        String body = buildMarkdownBody(ctx);
        webClient.post().uri(webhookUrl)
                .contentType(MediaType.APPLICATION_JSON)
                .bodyValue(body)
                .retrieve()
                .bodyToMono(String.class)
                .block(Duration.ofSeconds(10));
    }

    /**
     * 钉钉加签：timestamp + "\n" + secret 做 HMAC-SHA256，Base64 后 URLEncode
     */
    private String buildSignedUrl(String webhookUrl, String secret) {
        if (!StringUtils.hasText(secret)) return webhookUrl;
        long timestamp = System.currentTimeMillis();
        String stringToSign = timestamp + "\n" + secret;
        try {
            Mac mac = Mac.getInstance("HmacSHA256");
            mac.init(new SecretKeySpec(secret.getBytes(StandardCharsets.UTF_8), "HmacSHA256"));
            byte[] signData = mac.doFinal(stringToSign.getBytes(StandardCharsets.UTF_8));
            String sign = URLEncoder.encode(Base64.getEncoder().encodeToString(signData), StandardCharsets.UTF_8);
            return webhookUrl + "&timestamp=" + timestamp + "&sign=" + sign;
        } catch (Exception e) {
            throw new NotifyException("钉钉加签失败", e);
        }
    }
}
```

#### 3.4.2 EmailNotifySender

```java
@Component
public class EmailNotifySender implements NotifySender {

    @Autowired
    private JavaMailSender mailSender;

    @Override
    public ChannelType supportType() { return ChannelType.EMAIL; }

    @Override
    public void send(NotifyContext ctx) {
        EmailConfig config = ctx.channelConfig(EmailConfig.class);
        MimeMessage message = mailSender.createMimeMessage();
        MimeMessageHelper helper = new MimeMessageHelper(message, "UTF-8");
        helper.setFrom(config.getSenderAddr());
        helper.setTo(config.getReceiverAddrs().split(","));
        helper.setSubject(buildSubject(ctx));
        helper.setText(buildHtmlBody(ctx), true);
        mailSender.send(message);
    }
}
```

---

### 3.5 AES-256-GCM 加密实现

```java
@Component
public class AesEncryptUtil {

    @Value("${opencloud.monitor.encrypt-key}")
    private String encryptKey;

    private static final int GCM_IV_LENGTH = 12;
    private static final int GCM_TAG_LENGTH = 128;

    public String encrypt(String plaintext) {
        try {
            byte[] iv = new byte[GCM_IV_LENGTH];
            new SecureRandom().nextBytes(iv);
            Cipher cipher = Cipher.getInstance("AES/GCM/NoPadding");
            cipher.init(Cipher.ENCRYPT_MODE, getKey(),
                    new GCMParameterSpec(GCM_TAG_LENGTH, iv));
            byte[] cipherText = cipher.doFinal(plaintext.getBytes(StandardCharsets.UTF_8));
            // 拼接 IV + 密文，Base64 编码
            byte[] combined = new byte[GCM_IV_LENGTH + cipherText.length];
            System.arraycopy(iv, 0, combined, 0, GCM_IV_LENGTH);
            System.arraycopy(cipherText, 0, combined, GCM_IV_LENGTH, cipherText.length);
            return Base64.getEncoder().encodeToString(combined);
        } catch (Exception e) {
            throw new IllegalStateException("加密失败", e);
        }
    }

    public String decrypt(String ciphertext) {
        try {
            byte[] combined = Base64.getDecoder().decode(ciphertext);
            byte[] iv = Arrays.copyOfRange(combined, 0, GCM_IV_LENGTH);
            byte[] data = Arrays.copyOfRange(combined, GCM_IV_LENGTH, combined.length);
            Cipher cipher = Cipher.getInstance("AES/GCM/NoPadding");
            cipher.init(Cipher.DECRYPT_MODE, getKey(), new GCMParameterSpec(GCM_TAG_LENGTH, iv));
            return new String(cipher.doFinal(data), StandardCharsets.UTF_8);
        } catch (Exception e) {
            throw new IllegalStateException("解密失败", e);
        }
    }

    private SecretKeySpec getKey() {
        byte[] keyBytes = encryptKey.getBytes(StandardCharsets.UTF_8);
        byte[] key32 = new byte[32];
        System.arraycopy(keyBytes, 0, key32, 0, Math.min(keyBytes.length, 32));
        return new SecretKeySpec(key32, "AES");
    }
}
```

---

### 3.6 通知异步发送（@Async + 重试）

```java
@Service
@Slf4j
public class NotifyDispatchService {

    @Autowired private NotifyChannelMapper channelMapper;
    @Autowired private NotifyLogMapper logMapper;
    @Autowired private AesEncryptUtil aesEncryptUtil;
    @Autowired private List<NotifySender> senders;

    @Async("notifyExecutor")
    public void sendAsync(MonitorAlertRecord record, List<Long> channelIds) {
        for (Long channelId : channelIds) {
            MonitorNotifyChannel channel = channelMapper.selectById(channelId);
            if (channel == null || channel.getDeleted() == 1) continue;
            sendWithRetry(record, channel, false, 0);
        }
    }

    @Async("notifyExecutor")
    public void sendResolvedAsync(MonitorAlertRecord record, List<Long> channelIds) {
        sendAsync(record, channelIds);  // 恢复通知复用相同逻辑
    }

    private void sendWithRetry(MonitorAlertRecord record, MonitorNotifyChannel channel,
                               boolean isTest, int retryCount) {
        NotifySender sender = senders.stream()
                .filter(s -> s.supportType().name().equals(channel.getChannelType()))
                .findFirst()
                .orElseThrow(() -> new MonitorException(MONITOR_5001, "不支持的渠道类型"));
        try {
            NotifyContext ctx = buildContext(record, channel, isTest, aesEncryptUtil);
            sender.send(ctx);
            saveLog(record.getId(), channel, "SUCCESS", null, retryCount, isTest);
        } catch (Exception e) {
            log.warn("通知发送失败（渠道={}，重试={}）：{}", channel.getId(), retryCount, e.getMessage());
            if (retryCount < 3) {
                int delay = retryCount == 0 ? 30 : retryCount == 1 ? 60 : 120;
                scheduleRetry(record, channel, isTest, retryCount + 1, delay);
            } else {
                saveLog(record.getId(), channel, "FAILED", e.getMessage(), retryCount, isTest);
            }
        }
    }

    private void scheduleRetry(MonitorAlertRecord record, MonitorNotifyChannel channel,
                               boolean isTest, int nextRetry, int delaySec) {
        CompletableFuture.delayedExecutor(delaySec, TimeUnit.SECONDS)
                .execute(() -> sendWithRetry(record, channel, isTest, nextRetry));
    }
}

// 线程池配置（NotifySenderConfig）：
@Bean("notifyExecutor")
public Executor notifyExecutor(
        @Value("${opencloud.monitor.notify-async-pool-size:5}") int poolSize) {
    ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
    executor.setCorePoolSize(poolSize);
    executor.setMaxPoolSize(poolSize * 2);
    executor.setQueueCapacity(100);
    executor.setThreadNamePrefix("notify-");
    executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
    executor.initialize();
    return executor;
}
```

---

### 3.7 AlertRuleMapper 示例

```java
@Mapper
public interface AlertRuleMapper extends BaseMapper<MonitorAlertRule> {

    /** 按 alertName 匹配规则（用于 Webhook 处理时查找规则） */
    @Select("SELECT * FROM monitor_alert_rule WHERE tenant_id = #{tenantId} " +
            "AND rule_name = #{alertName} AND status = 'ENABLED' AND deleted = 0 LIMIT 1")
    MonitorAlertRule findByAlertName(@Param("alertName") String alertName,
                                    @Param("tenantId") String tenantId);

    /** 检查通知渠道是否被引用 */
    @Select("SELECT COUNT(*) FROM monitor_alert_rule " +
            "WHERE notify_channel_ids::jsonb @> CAST('[' || #{channelId} || ']' AS jsonb) " +
            "AND deleted = 0")
    int countByChannelId(@Param("channelId") Long channelId);
}
```

### 3.8 AlertRecordMapper XML 示例

```xml
<!-- mapper/alert/AlertRecordMapper.xml -->
<select id="selectPageVO" resultType="com.qoobot.opencloud.monitor.alert.record.domain.vo.AlertRecordVO">
    SELECT id, rule_id, rule_name, alert_name, severity, instance,
           summary, status, fired_at, resolved_at, ack_by, ack_at, ack_note, notified
    FROM monitor_alert_record
    WHERE tenant_id = #{tenantId}
    <if test="q.alertName != null and q.alertName != ''">
        AND alert_name LIKE CONCAT('%', #{q.alertName}, '%')
    </if>
    <if test="q.severity != null and q.severity != ''">
        AND severity = #{q.severity}
    </if>
    <if test="q.status != null and q.status != ''">
        AND status = #{q.status}
    </if>
    <if test="q.ruleId != null">
        AND rule_id = #{q.ruleId}
    </if>
    <if test="q.instance != null and q.instance != ''">
        AND instance LIKE CONCAT('%', #{q.instance}, '%')
    </if>
    <if test="q.beginTime != null">
        AND fired_at &gt;= #{q.beginTime}
    </if>
    <if test="q.endTime != null">
        AND fired_at &lt;= #{q.endTime}
    </if>
    ORDER BY fired_at DESC
</select>

<select id="findByDedup" resultType="com.qoobot.opencloud.monitor.alert.record.domain.entity.MonitorAlertRecord">
    SELECT * FROM monitor_alert_record
    WHERE alert_name = #{alertName}
      AND instance = #{instance}
      AND fired_at = #{firedAt}
    LIMIT 1
</select>

<select id="selectStats" resultType="com.qoobot.opencloud.monitor.alert.record.domain.vo.AlertStatsVO">
    SELECT
        COUNT(*) AS total,
        SUM(CASE WHEN status = 'FIRING' THEN 1 ELSE 0 END) AS firing,
        SUM(CASE WHEN status = 'ACKNOWLEDGED' THEN 1 ELSE 0 END) AS acknowledged,
        SUM(CASE WHEN status = 'RESOLVED' THEN 1 ELSE 0 END) AS resolved,
        SUM(CASE WHEN severity = 'CRITICAL' THEN 1 ELSE 0 END) AS critical_count,
        SUM(CASE WHEN severity = 'WARNING' THEN 1 ELSE 0 END) AS warning_count,
        SUM(CASE WHEN severity = 'INFO' THEN 1 ELSE 0 END) AS info_count,
        AVG(CASE WHEN status = 'RESOLVED'
            THEN EXTRACT(EPOCH FROM (resolved_at - fired_at))
            ELSE NULL END) AS avg_duration_seconds
    FROM monitor_alert_record
    WHERE tenant_id = #{tenantId}
      AND fired_at &gt;= #{beginTime}
</select>
```

---

### 3.9 MonitorException 定义

```java
public class MonitorException extends RuntimeException {

    private final int code;
    private final String message;

    // 工厂方法
    public static MonitorException prometheusUnavailable() {
        return new MonitorException(1001, "监控服务暂时不可用，请稍后重试");
    }
    public static MonitorException promqlError(String detail) {
        return new MonitorException(1002, "PromQL 表达式无效：" + detail);
    }
    public static MonitorException queryTimeout() {
        return new MonitorException(1003, "查询超时，请缩小时间范围或优化查询语句");
    }
    public static MonitorException timeRangeExceeded() {
        return new MonitorException(1004, "时间范围不能超过 30 天");
    }
    public static MonitorException grafanaUnavailable() {
        return new MonitorException(2001, "Grafana 服务暂时不可用");
    }
    public static MonitorException dashboardNotFound() {
        return new MonitorException(2002, "面板不存在或已被删除");
    }
    public static MonitorException ruleNameDuplicate() {
        return new MonitorException(3001, "规则名称已存在");
    }
    public static MonitorException invalidPromql(String detail) {
        return new MonitorException(3002, "PromQL 表达式无效：" + detail);
    }
    public static MonitorException ruleNotFound() {
        return new MonitorException(3003, "告警规则不存在");
    }
    public static MonitorException recordNotFound() {
        return new MonitorException(4001, "告警记录不存在");
    }
    public static MonitorException alreadyAcked() {
        return new MonitorException(4002, "告警已确认，无需重复操作");
    }
    public static MonitorException invalidStatusTransition() {
        return new MonitorException(4003, "当前状态不允许此操作");
    }
    public static MonitorException channelNotFound() {
        return new MonitorException(5001, "通知渠道不存在");
    }
    public static MonitorException channelInUse(int count) {
        return new MonitorException(5002, "该渠道被 " + count + " 条告警规则引用，请先解绑");
    }
    public static MonitorException webhookSignatureInvalid() {
        return new MonitorException(6001, "Webhook 签名验证失败");
    }
    public static MonitorException webhookPayloadInvalid() {
        return new MonitorException(6002, "Webhook payload 格式非法");
    }
}
```

---

## 4. MyBatis-Plus 使用规范

| 项 | 规范 |
|----|------|
| 实体注解 | `@TableName("monitor_xxx")`，自增 ID 用 `@TableId(type = IdType.AUTO)` |
| 逻辑删除 | `monitor_alert_rule` / `monitor_notify_channel` 有 `@TableLogic` 字段 `deleted` |
| 自动填充 | `created_at` / `updated_at` 通过 `MetaObjectHandler` 自动填充（复用 system 模块的 MetaObjectHandler 或新建） |
| 分页 | 使用 `Page<T>` + `IPage<VO>` 模式；Mapper XML 中 `selectPageVO` 返回 `IPage<VO>` |
| 租户隔离 | `TenantLineInnerInterceptor` 对 `monitor_alert_rule`、`monitor_alert_record`、`monitor_notify_channel`、`monitor_notify_log`、`monitor_alert_silence` 启用；`TenantContext.get()` 返回当前租户 ID |

---

## 5. 测试覆盖要点

| 测试点 | 测试类型 | 关键场景 |
|--------|---------|---------|
| PrometheusClient | 单元测试（MockWebServer） | Prometheus 正常返回 / error 状态 / 超时 / 不可达 |
| AlertWebhookServiceImpl | 单元测试 | firing 幂等 / resolved 更新 / 命中静默 / 未匹配规则 |
| AesEncryptUtil | 单元测试 | 加密解密正确性 / 不同 IV 每次不同 |
| DingTalkNotifySender | 单元测试（MockWebServer） | 加签 URL 正确性 / 请求体格式 |
| NotifyDispatchService | 单元测试 | 重试逻辑（失败 3 次后写 FAILED log） |
| AlertWebSocketHandler | 集成测试 | Token 无效拒绝连接 / 同用户多连接替换 / 广播 |
| AlertRuleServiceImpl | 单元测试 | PromQL 校验 / 规则名唯一 / 渠道引用检查 |

---

## 6. 安全 Checklist

| 项 | 状态 | 说明 |
|----|------|------|
| WebSocket Token 验证 | ✅ | 握手时校验 JWT，无效拒绝连接 |
| Webhook 签名验证 | ✅ | 可配置，空 secret 时不校验（内网部署场景） |
| 通知渠道敏感字段加密 | ✅ | AES-256-GCM，密钥通过环境变量注入 |
| 接口权限控制 | ✅ | `@PreAuthorize("hasAuthority('...')")` |
| PromQL 安全 | ⚠️ | 未限制 PromQL 内容，需确保仅授权用户可调用查询接口 |
| Prometheus/Grafana 地址隔离 | ✅ | 不暴露给前端，通过服务端代理 |
| 通知内容脱敏 | ✅ | 告警通知中不包含密钥、密码等敏感信息 |

---

*文档维护人：开发团队 | 下次评审：待排期*
