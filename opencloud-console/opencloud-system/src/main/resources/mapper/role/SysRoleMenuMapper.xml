<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qoobot.opencloud.system.role.mapper.SysRoleMenuMapper">

    <!-- 查询角色已分配的菜单 ID 列表 -->
    <select id="selectMenuIdsByRoleId" resultType="java.lang.Long">
        SELECT menu_id FROM sys_role_menu WHERE role_id = #{roleId}
    </select>

    <!-- 根据角色 ID 删除关联数据 -->
    <delete id="deleteByRoleId">
        DELETE FROM sys_role_menu WHERE role_id = #{roleId}
    </delete>

    <!-- 查询角色下的用户 ID 列表（角色菜单变更时批量清缓存） -->
    <select id="selectUserIdsByRoleId" resultType="java.lang.String">
        SELECT DISTINCT user_id FROM sys_user_role WHERE role_id = #{roleId}
    </select>

</mapper>
