<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qoobot.opencloud.system.dict.mapper.SysDictTypeMapper">

    <!-- 分页查询字典类型 -->
    <select id="selectPageVO" resultType="com.qoobot.opencloud.system.dict.domain.vo.DictTypeVO">
        SELECT id, dict_type, dict_name, is_system, status, remark, created_at
        FROM sys_dict_type
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (dict_name LIKE CONCAT('%', #{keyword}, '%')
            OR dict_type LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>
