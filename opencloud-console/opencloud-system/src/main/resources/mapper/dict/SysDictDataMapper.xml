<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qoobot.opencloud.system.dict.mapper.SysDictDataMapper">

    <!-- 按字典类型查询字典数据列表 -->
    <select id="selectByDictType" resultType="com.qoobot.opencloud.system.dict.domain.vo.DictDataVO">
        SELECT id, dict_type, dict_label, dict_value, sort, is_default, status, remark
        FROM sys_dict_data
        WHERE dict_type = #{dictType}
          AND deleted = 0
          AND status = 'ACTIVE'
        ORDER BY sort ASC, id ASC
    </select>

</mapper>
